{
  "description": "Route one queued Synapse message from the main project checkout.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node ${PLUGIN_ROOT}/hooks/dispatch.mjs",
            "statusMessage": "Checking Synapse inbox"
          }
        ]
      }
    ]
  }
}
